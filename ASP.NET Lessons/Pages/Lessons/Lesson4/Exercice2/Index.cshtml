@page
@model ASP.NET_Lessons.Pages.Lessons.Lesson4.Exercice2.IndexModel
@{
}

<h1>User Cards</h1>
<div style="display: flex; gap: 10px; flex-wrap:wrap">
    @foreach (var user in Model.Users)
    {
        @await Html.PartialAsync("User/_UserCard", user)
    }
</div>